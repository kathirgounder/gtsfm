#!/usr/bin/env bash
# Launcher for the GTSFM pipeline trace visualizer.
# Sibling to ./viz — separate Flask app on port 5174.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python "$SCRIPT_DIR/pipeline_visualization/server.py" "$@"
